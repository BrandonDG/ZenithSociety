@model IEnumerable<Microsoft.AspNetCore.Identity.EntityFrameworkCore.IdentityRole>
@{
    ViewData["Title"] = "Revoke Role";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Revoke Role</h2>

<form asp-action="RevokeRole">
    <p>@ViewBag.Test</p>
    <input type="hidden" name="userId" value="@ViewBag.id" />
    <div class="form-horizontal">
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <label>Roles</label>
        <div class="form-group">
            <select name="roleId" class="form-control col-md-10">
                @foreach (var role in Model)
                {
                    <option value="@role.Id">@role.Name</option>
                }
            </select>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Revoke Role" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>